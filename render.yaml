services:
  - type: web
    name: indian-law-chatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run Chatbot.py --server.port=$PORT --server.address=0.0.0.0
    envVars:
      - key: GEMINI_API_KEYS
        sync: false
